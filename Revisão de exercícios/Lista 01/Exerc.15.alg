algoritmo "Exerc.15"
var
    temp: real
inicio
    escreva("Digite a temperatura em radianos para ser convertida em graus: ")
    leia(temp)
    limpatela()
    escreva(temp, " radianos, equivale a: ", ((temp*180)/pi), " graus")
fimalgoritmo
