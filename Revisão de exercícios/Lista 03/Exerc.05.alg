algoritmo "Exerc.5"
var
    c,soma,num: inteiro
inicio

    para c <- 1 ate 10 passo 1 faca
        escreva("Digite um valor para ser somado: ")
        leia(num)
        soma <- soma + num
    fimpara
    escreva("O valor da soma é: ", soma)
fimalgoritmo
